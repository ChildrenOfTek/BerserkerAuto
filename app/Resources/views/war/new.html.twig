{% extends 'base.html.twig' %}

{% block section %}
    <h1>Création d'une war</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('war_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
